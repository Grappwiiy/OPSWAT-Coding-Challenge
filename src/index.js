import React from 'react';
import ReactDOM from 'react-dom/client';
import OPSWAT from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <OPSWAT />
  </React.StrictMode>
);

